# App
APP_ENV=development
DATABASE_URL=sqlite+aiosqlite:///./waziride.db
BASE_FARE_KES=200
PER_KM_KES=60
PER_MIN_KES=4

# Twilio (WhatsApp + SMS)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
TWILIO_SMS_FROM=

# Africa's Talking (SMS)
AT_USERNAME=sandbox
AT_API_KEY=
AT_SMS_SHORTCODE=

# Optional geocoding (Google Maps) — used if set, otherwise mock geocoder is used
GOOGLE_MAPS_API_KEY=
